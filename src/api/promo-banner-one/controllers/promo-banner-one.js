'use strict';

/**
 * promo-banner-one controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::promo-banner-one.promo-banner-one');
